# GenAI Hub — Prototype

Prototype statique (HTML/CSS/JS, fichier unique) pour l'interface GenAI Hub.

## Aperçu en ligne

Publié automatiquement via GitHub Pages à chaque push sur `main` :
https://thibaudbreton.github.io/REX/

## Développement local

Ouvrir `index.html` directement dans un navigateur, aucune dépendance ni build requis.
